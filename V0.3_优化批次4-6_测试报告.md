# V0.3 优化批次 4-6 性能测试报告

**测试日期**: 2026-07-14
**测试环境**: Windows 10, GPT-SoVITS api_v2.py :9880 (V2Pro GPU), sherpa-onnx 1.12.29
**前置**: 优化批次 1-3 已完成（TTS 缓存/VAD 调参/prompt 精简）

---

## 一、本轮优化项与结论

| 优化项 | 状态 | 结论 |
|---|---|---|
| **优化4: 流式 ASR (sherpa-onnx)** | ⚠️ 已验证未启用 | sherpa 速度快（48-109ms vs FunASR 400ms）但准确率明显差，陪伴对话场景准确率优先，保留 FunASR。streaming_asr.py 保留待模型升级。 |
| **优化5: GPT-SoVITS 流式合成** | ✅ 完成 | api_v2.py :9880 streaming_mode=1，首包 0.45-0.98s（均值 0.64s），对比整句 2s 省 ~1.36s。 |
| **优化6: 首句更激进切分** | ✅ 完成 | core.py 加无标点长句兜底（首句超 10 字强制切），覆盖 LLM 输出无标点的 edge case。 |

---

## 二、优化4 详述：流式 ASR 验证结论（跳过）

### 测试数据（三段音频对比）

| 音频 | sherpa 流式 (int8 双语) | FunASR 整段 | 正确答案 |
|---|---|---|---|
| test_full.wav (3.7s) | "啊今天嘴子里这东西填你是不好" (0.076s) | "哎，今天嘴这么甜，你说不哈哈哈。" (0.464s) | FunASR 对 |
| test_tts.wav (2.0s) | "蛋蛋怎" (0.048s) | "站了怎么了？到位？" (0.408s) | FunASR 对 |
| voice_reply.wav (4.0s) | "谢谢十三十二好好..." (0.109s) | "7月12号眼唱..." (0.480s) | 都不准 |

### 结论
- **速度**: sherpa 快 4-5 倍（省 ~300-400ms）
- **准确率**: sherpa 明显差，短句几乎全错；陪伴对话场景识别错会导致 LLM 回复跑偏
- **决策**: 用户决定保留 FunASR，跳过优化4。streaming_asr.py 模块已写好并验证可用，标注未启用，待后续更大流式模型（如 paraformer 流式）验证后启用。

---

## 三、优化5 详述：GPT-SoVITS 流式合成

### 实现方案
1. **另起 api_v2.py :9880**（gsv-env，V2Pro GPU 模型，与 tts_server.py :8880 独立进程）
2. **ling 改 tts_synthesize_stream()**: 流式请求 :9880 streaming_mode=1，逐 chunk 收 PCM
3. **32kHz→16kHz 重采样**: api_v2 输出 32kHz，Opus 编码器要 16kHz，逐 chunk scipy 多相重采样
4. **int16 对齐处理**: 流式 chunk 边界奇数字节暂存 leftover 拼接，防 buffer size 错误
5. **边收边 Opus 编码发送**: 每 PCM chunk → OpusEncoder → 逐包 send_bytes + 流控 + 打断检查
6. **三路径选择**: 缓存命中(整句Opus直发) > 流式可用(边收边发) > 回退整句(原逻辑)
7. **warmup 改用 :9880**: 预热也走 api_v2（:8880 没起时也能工作）

### 四种 streaming_mode 延迟对比（同文本"宝贝想你了亲一个么么哒"）

| 模式 | 首包延迟 | 总耗时 | 质量档 | 说明 |
|---|---|---|---|---|
| mode=0 非流式 | 2.207s | 2.208s | 最佳 | 等整段合成（ling 原等效） |
| **mode=1 旧版分段** | **1.176s** | 1.391s | **最佳** | **ling 采用，质量同 mode=0** |
| mode=2 真流式 | 1.320s | 1.568s | 中 | semantic token 流式解码 |
| mode=3 定长分块 | 0.512s | 1.318s | 低 | 太快但音质降，不采用 |

### 端到端流式 TTS 首包延迟（含重采样+Opus编码）

| 文本 | 首Opus包 | 总耗时 | Opus包数 | 音频时长 |
|---|---|---|---|---|
| 在呢怎么了 | 0.975s | 1.102s | 35 | 2.10s |
| 宝贝想你了 | 0.445s | 0.552s | 22 | 1.32s |
| 今天好累啊不想动 | 0.501s | 0.607s | 40 | 2.40s |
| **均值** | **0.640s** | - | - | - |

重采样 + Opus 编码开销极小（首PCM chunk → 首 Opus 包仅多 ~4ms）。

---

## 四、组件级延迟基线

| 组件 | 延迟 | 说明 |
|---|---|---|
| VAD 单帧 (SileroVAD) | 0.224 ms | 512 样本/32ms 块 |
| Opus 编码 1s PCM | 5.257 ms | 17 包，complexity 10 |
| Opus 解码单包 | 0.040 ms | 960 样本 |
| TTS 缓存命中 | 0.000 ms | 整句 Opus 包直发 |

---

## 五、理论首字延迟推算（用户说完到听到声音）

### 延迟链路
```
用户说完 → VAD端点检测 → FunASR识别 → LLM首句 → TTS首包 → 用户听到
```

### 各环节延迟

| 环节 | 延迟 | 说明 |
|---|---|---|
| VAD 端点检测 | 600 ms | VAD_SILENCE_MS（说完后静音判定） |
| FunASR 识别 | 400 ms | 整段识别（实测，保留不用流式） |
| LLM 首句 | 800 ms | 历史基线（prompt 精简后，首句激进切分） |
| 流式 TTS 首包 | 640 ms | 均值（streaming_mode=1） |
| TTS 缓存命中 | 0 ms | 短回复命中 |

### 首字延迟对比

| 场景 | 计算 | 首字延迟 | 对比 V0.3 原始 |
|---|---|---|---|
| **V0.3 原始**（整句 TTS 2000ms） | 600+400+800+2000 | **3.80s** | 基线 |
| **优化后非缓存** | 600+400+800+640 | **2.44s** | 省 1.36s |
| **优化后缓存命中** | 600+400+800+0 | **1.80s** | 省 2.00s |

---

## 六、改动文件清单

### 后端
| 文件 | 改动 |
|---|---|
| `backend/streaming_asr.py` | **新建** sherpa-onnx 流式 ASR 封装，已验证可用但未启用（准确率不达标） |
| `backend/ws_server.py` | 加 `tts_synthesize_stream()` 流式合成 + `_resample_pcm_32k_to_16k()` + `_send_opus_packets()` + process_turn 三路径(缓存/流式/回退) + warmup 改用 :9880 |
| `backend/core.py` | chat_stream 首句切分加无标点长句兜底（FIRST_SEGMENT_MAX_CHARS=10） |
| `backend/config.py` | 加 TTS_STREAMING_URL / TTS_STREAMING_MODE / TTS_REF_AUDIO / TTS_REF_TEXT |

### 模型文件（已下载）
- `Create-Ex/xiaozhi-server/.../models/sherpa-onnx-streaming-zipformer-zh-14M-2023-02-23/`（int8+fp32，~65MB，未启用）
- `Create-Ex/xiaozhi-server/.../models/sherpa-onnx-streaming-zipformer-bilingual-zh-en-2023-02-20/`（int8，~200MB，未启用）

---

## 七、启动方式变更

### 新增服务：GPT-SoVITS 流式 TTS :9880
```bash
cd "G:/personal project/Create-Ex/GPT-SoVITS"
"G:/personal project/Create-Ex/gsv-env/Scripts/python.exe" api_v2.py -p 9880 -c GPT_SoVITS/configs/tts_infer.yaml
```

### 服务清单（V0.3 优化后）
| 服务 | 端口 | 用途 | 必需 |
|---|---|---|---|
| api_server.py | 8765 | HTTP API（文字聊天/朋友圈） | 是 |
| ws_server.py | 8766 | WebSocket 语音通话 | 语音通话时 |
| **api_v2.py** | **9880** | **GPT-SoVITS 流式 TTS** | **语音通话时（新）** |
| tts_server.py | 8880 | GPT-SoVITS 整句 TTS（warmup fallback） | 可选 |

> :9880 流式 TTS 是语音通话首字延迟的关键优化。:8880 现仅作为 warmup fallback，不启动也不影响（warmup 会用 :9880）。

---

## 八、未优化空间（V0.4+）

1. **流式 ASR**: sherpa 14M/双语 int8 准确率不达标，待更大流式模型（paraformer 流式 / zipformer-large）验证
2. **LLM 首句延迟**: 800ms 占比大，可考虑更激进的首字策略（遇 3-5 字即切）但音质会降
3. **VAD 端点 600ms**: 可调更短（如 400ms）但易误触发，需实测
4. **FunASR 流式模式**: 起 FunASR WebSocket server 边说边识别，兼顾速度和准确率，改动较大
