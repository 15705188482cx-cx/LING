[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# 测试只 import 纯逻辑模块（text_utils/errors/idempotency），不加载模型
# 但 backend 目录在 sys.path 里，import config 会触发 sys.path 注入——
# 测试不 import config/core，避免拉起 FAISS/DB/LLM
addopts = -q
