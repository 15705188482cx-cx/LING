# 刘嘉玲 Ling V0.2 测试大纲

> **用途**：交接给 GPT + 用户协同测试。A 类后端自测我（Claude）已跑通，B/C 类需用户在浏览器手动验证。
>
> **生成时间**：2026-07-13 ｜ 后端版本：V0.2（含朋友圈 + 限流 + 统一信封 + 健康分级）｜ 前端版本：`feat/moments`
>
> **服务端口**：api_server `:8765` ｜ ws_server `:8766` ｜ tts_server `:8880` ｜ 前端 dev `:5173`

---

## 目录

1. [测试方式分类](#1-测试方式分类)
2. [服务启动前置](#2-服务启动前置)
3. [A 类：后端自动测试（已测通过）](#3-a-类后端自动测试已测通过)
4. [A 类：可补测项](#4-a-类可补测项)
5. [B 类：前端手动测试（不消耗云 token）](#5-b-类前端手动测试不消耗云-token)
6. [B 类：语音测试（需 ws_server，不消耗云 token）](#6-b-类语音测试需-ws_server不消耗云-token)
7. [B 类：图片测试（消耗 VLM+LLM token）](#7-b-类图片测试消耗-vlmllm-token)
8. [C 类：视频通话（V0.3 再测）](#8-c-类视频通话v03-再测)
9. [Token 消耗明细](#9-token-消耗明细)
10. [建议测试顺序](#10-建议测试顺序)
11. [问题反馈模板](#11-问题反馈模板)

---

## 1. 测试方式分类

| 类别 | 谁测 | 手段 | 覆盖范围 |
|---|---|---|---|
| **A 类** | Claude（自动） | curl / Python 脚本直连后端 :8765 | 接口逻辑、数据持久化、错误处理、限流 |
| **B 类** | 用户（手动） | 浏览器 `pnpm dev` + 真实硬件 | UI 交互、麦克风/摄像头、主观体验 |
| **C 类** | 用户（手动） | 起多服务后浏览器操作 | 通话链路（依赖 ws_server:8766 + tts:8880） |

> **原则**：A 类已覆盖到底，B/C 类需用户点鼠标和开摄像头。每步都标注了"该看到什么"。

---

## 2. 服务启动前置

### 2.1 最小启动（测 A 类 + B 类前端交互）

```bash
# 1. 起 api_server（:8765）—— 朋友圈/聊天/profile 全在这里
cd "G:/personal project/ling/backend"
"G:/personal project/Create-Ex/voice-env/Scripts/python.exe" api_server.py

# 2. 起前端 dev（:5173）
cd "G:/personal project/ling/webview"
pnpm dev
```

验证：
```bash
curl http://127.0.0.1:8765/health
# 期望 {"status":"ok","memory":true,"db":true,"user_id":16}
```

### 2.2 完整启动（测语音/视频，需 ws_server + tts_server）

```bash
# 额外起 ws_server（:8766）—— ASR + 语音/视频通话
cd "G:/personal project/ling/backend"
"G:/personal project/Create-Ex/voice-env/Scripts/python.exe" ws_server.py

# 额外起 tts_server（:8880）—— GPT-SoVITS 语音合成
# 通过 Create-Ex control.bat 选 tts 启动项
```

> ⚠️ 三个服务一起跑内存压力大（FAISS + FunASR + GPT-SoVITS），16G 机器可能吃紧。先测 A+B 前端，再单独起 ws_server 测语音。

---

## 3. A 类：后端自动测试（已测通过）

> 以下由 Claude 自动测试，结果全部通过。用户可跳过，也可用 `test_moments.py` 复现。

### 3.1 朋友圈闭环测试 ✅

**脚本**：`backend/test_moments.py`
**结果**：12 场景 61 断言全通过

| 场景 | 测试内容 | 结果 |
|---|---|---|
| [0] 健康检查 | `/health` 返回 status/memory/db | ✅ |
| [1] 初始列表 | `GET /moments` 返回 items/has_more，结构完整 | ✅ |
| [2] 发圈带图 | `POST /moments` 带 base64 图片，返回 ok+id | ✅ |
| [3] 空内容校验 | content="" → 422 INVALID_INPUT 信封 | ✅ |
| [4] 图片持久化 | 发圈后列表复查，images 是 base64 data URL | ✅ |
| [5] 点赞幂等 | 切换：赞→取消→赞，count 正确 | ✅ |
| [6] 评论+LLM | 评论触发 LLM 回复，reply 非空，emotion 是 5 枚举之一 | ✅ |
| [7] 评论持久化 | 列表复查评论带 reply/reply_emotion | ✅ |
| [8] 红点计数 | since=0 有数，since=现在 为 0 | ✅ |
| [9] 配置读写 | POST 改 120 → GET 读回 120 | ✅ |
| [10] 不存在的朋友圈 | 评论不存在 ID → 错误信封；点赞不崩 | ✅ |
| [11] 限流 | 发圈第 6 条 → 429 UPSTREAM_RATE_LIMITED | ✅ |
| [12] 分页 | limit=2 + before 参数返回更早的 | ✅ |

复现命令：
```bash
cd "G:/personal project/ling/backend"
"G:/personal project/Create-Ex/voice-env/Scripts/python.exe" test_moments.py
```

### 3.2 /chat 文字对话 ✅

| 测试项 | 结果 |
|---|---|
| 正常回复 | ✅ `{ok:true, request_id, reply, emotion}` |
| 幂等去重 | ✅ 同 client_message_id 秒回 30ms，带 `idempotent:true` |
| CONTENT_BLOCKED | ✅ 审核拦截返回 `retryable:false` |
| 输入校验 | ✅ 空文本/超长 → 422 INVALID_INPUT |
| 错误信封 | ✅ `{ok:false, request_id, error:{code,message,retryable}}` |

### 3.3 其他已测接口 ✅

| 接口 | 状态 |
|---|---|
| `GET /health` 分级（ok/degraded） | ✅ |
| `GET/PUT /profile` 持久化 | ✅ |
| `GET /history` `POST /reset` | ✅ |
| `/chat/image` `/video/frame` 失败统一信封 | ✅（代码改完，未触发失败场景实测） |

---

## 4. A 类：可补测项

> 以下 Claude 可补测但尚未跑。需用户确认是否消耗 token。

| # | 模块 | 测试内容 | 消耗 token? | 预估 token |
|---|---|---|---|---|
| 9 | `/chat/image` | 发 base64 图片 → VLM 描述 → LLM 回复 | **是**（VLM+LLM） | ~8800/次 |
| 10 | `/video/frame` | 模拟视频帧 → VLM → 回复 | **是**（VLM+LLM） | ~8800/次 |
| 11 | `/tts` | 文字 → wav 字节流 | 否（本地 GPU） | 0 |
| 12 | 评论 LLM 兜底 | 模拟 LLM 失败 → 兜底回复 | 否（要 LLM 挂才触发） | 0 |
| 13 | 定时发圈线程 | 改频率 60s → 等 60s → 自动发圈 | 模板不消耗 | 0 |
| 14 | 模板耗尽 → 重置 | 标 15 条 used=1 → 触发 reset | 一次 LLM | ~4100 |

> 补测 9+10+14 约消耗 **2.6 万 token**。用户确认后再跑。

---

## 5. B 类：前端手动测试（不消耗云 token）

> 前提：api_server:8765 + 前端 dev:5173 已起。
> 打开 `http://localhost:5173`。

### 5.1 基础导航（0 token）

| # | 功能 | 操作 | 该看到 | 通过? |
|---|---|---|---|---|
| 15 | 4 Tab 切换 | 底部点 消息/通讯录/发现/我 | 切换正常，无白屏 | ☐ |
| 16 | 朋友圈列表 | 发现 → 朋友圈 | 3 条种子（台球/烧烤/王者荣耀），**不再是 mock** | ☐ |

### 5.2 朋友圈交互（评论消耗 ~5000 token/次，其他 0）

| # | 功能 | 操作 | 该看到 | 通过? |
|---|---|---|---|---|
| 17 | 点赞 | 点红心 | 红心亮起，总数 +1；再点取消，总数 -1 | ☐ |
| 18 | 评论 | 输入"厉害啊"发送 | 你的评论出现，几秒后**她回复出现**（如"哈哈 随便测测嘛"） | ☐ |
| 19 | 发圈（纯文字） | 顶部相机 → 写字 → 发 | 发完列表刷新出现新条目 | ☐ |
| 20 | **发圈带图** ⭐ | 选图 → 写字 → 发 → **刷新页面** | 发完列表出现；**刷新后图片仍在**（base64 持久化，本轮修复重点） | ☐ |
| 21 | 发圈频率配置 | 我 → 改频率 → 刷新 | 值保持（如改 60，刷新后还是 60） | ☐ |
| 22 | 红点轮询 | 退到发现 tab，等 30s | 过一会儿朋友圈入口出红点（如有新动态） | ☐ |

> ⭐ 第 20 项是本轮修复重点：之前图片用 blob URL，刷新就 404。现在改 base64 持久化，**务必验证刷新后图片还在**。

### 5.3 聊天交互（发消息消耗 ~8300 token/次，其他 0）

| # | 功能 | 操作 | 该看到 | 通过? |
|---|---|---|---|---|
| 23 | 文字对话 | 输入"在干嘛"→ 发 | 她回复，头像按 emotion 切换 | ☐ |
| 24 | 失败重试 | 断网/关后端发一条 → 恢复 → 点重试 | 失败气泡可点重试，重试后成功 | ☐ |
| 25 | CONTENT_BLOCKED | 发敏感词（如"在吗"偶发触发） | 她婉拒（"哎呀 这个不想聊"），**不显示系统错误** | ☐ |
| 26 | 长按消息菜单 | 长按一条消息 | 复制/引用/撤回/多选/删除菜单 | ☐ |
| 27 | 消息撤回 | 发一条 → 2 分钟内长按撤回 | 消息变成"你撤回了一条消息" | ☐ |
| 28 | 引用回复 | 长按 → 引用 → 发新消息 | 带引用块的消息 | ☐ |
| 29 | 拍一拍 | 双击对方头像 | "你拍了拍刘嘉玲"提示 | ☐ |
| 30 | 多选模式 | 长按 → 多选 → 选几条 | 可批量转发/删除 | ☐ |
| 31 | 草稿持久化 | 输入框打字不发 → 切走 → 切回 | 字还在，列表显"[草稿]" | ☐ |
| 32 | 表情面板 | 聊天页点表情图标 | 4 分类 emoji 网格 | ☐ |

### 5.4 个人资料（0 token）

| # | 功能 | 操作 | 该看到 | 通过? |
|---|---|---|---|---|
| 33 | 改名字 | 我 → 编辑资料 → 改名 → 保存 | 刷新后名字保持 | ☐ |
| 34 | 改签名 | 同上改签名 | 刷新后签名保持 | ☐ |
| 35 | 改头像 | 选图 → 保存 | 头像更新，刷新后保持 | ☐ |
| 36 | 深色模式/字体 | 我 → 设置 | 界面切换 | ☐ |

---

## 6. B 类：语音测试（需 ws_server，不消耗云 token）

> 前提：额外起 `ws_server.py`（:8766，加载 FunASR）。
> 消耗：本地 GPU，**0 云 token**。

| # | 功能 | 操作 | 该看到 | 通过? |
|---|---|---|---|---|
| 37 | 按住说话 | 聊天 🎤 按住说话 → 松开 | 识别文字进输入框，可编辑后发 | ☐ |
| 38 | 上滑取消 | 按住说话 → 上滑 | 录音中断，不发送 | ☐ |
| 39 | 语音转文字 | 发一条语音 → 长按 → 转文字 | 语音气泡下显示文字 | ☐ |

---

## 7. B 类：图片测试（消耗 VLM+LLM token）

> 前提：api_server:8765 已起。
> 消耗：每次约 **8800 token**（VLM 描述 + LLM 对话）。

| # | 功能 | 操作 | 该看到 | 通过? | token |
|---|---|---|---|---|---|
| 40 | 发图片给 AI | 聊天 + → 相册 → 选图 → 发 | 她看懂图并回复（如发猫，她说"猫猫好可爱"） | ☐ | ~8800 |
| 41 | 图片+文字 | 选图 + 输入"好看吗" → 发 | 她结合图和文字回复 | ☐ | ~8800 |

---

## 8. C 类：视频通话（V0.3 再测）

> ⚠️ **现状诚实说明**：ws_server 只修过崩溃 bug（send_json/send_bytes），功能逻辑没碰也没真跑过。GPT 提的 P0 问题（打断不可靠、TTS 重叠、turn_id 缺失）留 V0.3 重做。**现在测出问题也修不了，建议跳过。**

| # | 测试项 | 风险 | V0.3 是否重做 |
|---|---|---|---|
| 42 | 进通话页不崩 | 摄像头权限拒绝可能卡死 | 是 |
| 43 | 说话 → 她流式回复 | 可能 TTS 重叠/断续 | 是 |
| 44 | 打断（她说话时你开口） | ⚠️ 大概率不可靠（没拆 asyncio task） | 是 |
| 45 | 挂断后无残留 | ⚠️ 录音器/媒体轨可能没释放 | 是 |
| 46 | 静音/扬声器按钮 | ⚠️ 可能只改 UI 不控媒体轨 | 是 |

---

## 9. Token 消耗明细

### 9.1 每次操作消耗多少

| 操作 | 消耗什么 | 估算 token | 备注 |
|---|---|---|---|
| **/chat 一次** | MiniMax-M3 | input ~8000 + output ~300 | system 人格~4000 + history~3000 + memory~700 + 你的话~70 |
| **/chat/image 一次** | VLM + LLM | VLM~500 + LLM~8300 | 图片描述 + 一次完整对话 |
| **/video/frame 一次** | VLM + LLM | 同上 ~8800 | 视频帧当图处理 |
| **朋友圈评论一次** | MiniMax-M3 | input ~4500 + output ~150 | 人格 + 朋友圈正文 + 评论，不读 history |
| **定时发圈（模板）** | 无 | 0 | 从模板库选 |
| **定时发圈（LLM 兜底）** | MiniMax-M3 | input ~4000 + output ~100 | 模板用完才触发，现已改为循环复用 |
| /tts | 无云 token | 0 | 本地 GPT-SoVITS GPU |
| /asr | 无云 token | 0 | 本地 FunASR GPU |
| /moments 列表/点赞/配置 | 无 | 0 | 纯 SQLite |
| /history /reset /profile | 无 | 0 | 纯 SQLite |
| /health | 无 | 0 | — |

### 9.2 一次完整测试会消耗多少

| 场景 | 操作 | 消耗 token |
|---|---|---|
| **A 类全测**（Claude 跑） | test_moments.py + 几次 /chat | ~1.5 万 |
| **B1 前端交互**（用户测） | 大部分走 mock/已测接口 | ~0（评论几次约 5000 token） |
| **B2 按住说话**（用户测） | 3-5 次 ASR | 0（本地 GPU） |
| **B3 发图**（用户测） | 2-3 次图片对话 | ~2.5 万 |
| **C 视频通话**（用户测） | 每轮说话 = 1 次流式 LLM | 每分钟约 1 万（看聊多久） |

### 9.3 定时发圈的隐性消耗

| 频率档 | 间隔 | 一天发圈数 | 一天 LLM 调用 | 一天 token |
|---|---|---|---|---|
| 60s（测试档） | 1 分钟 | 1440 | 0（模板循环复用） | **0** |
| 300s（默认） | 5 分钟 | 288 | 0 | **0** |
| 1800s | 30 分钟 | 48 | 0 | **0** |
| 7200s | 2 小时 | 12 | 0 | **0** |
| 10800s | 3 小时 | 8 | 0 | **0** |

> 改完后模板循环复用，**定时发圈不再消耗 LLM token**（只有模板首次全部用完那一轮触发 1 次 LLM 生成 + 重置）。**但用户每评论一次她朋友圈 = 1 次 LLM ≈ 5000 token。**

---

## 10. 建议测试顺序

```
第 1 步（Claude 测，~2 分钟，消耗 ~1.5 万 token）
  → 跑 test_moments.py + 补测 /chat/image、/tts
  → 确认后端全绿
  → 已完成 ✅

第 2 步（用户测，~15 分钟，消耗 ~0 token）
  → pnpm dev 打开浏览器
  → 走 5.1 + 5.2 + 5.3 + 5.4
  → 重点验：发圈带图 → 刷新页面 → 图片还在不在（#20）
  → 重点验：评论后她回复出现（#18）

第 3 步（用户测，~5 分钟，消耗 0 token，需起 ws_server）
  → 起 ws_server:8766
  → 走第 6 节按住说话
  → 验录音 → 文字 → 发送

第 4 步（用户测，~10 分钟，消耗 ~2.5 万 token）
  → 走第 7 节发图片给 AI

第 5 步（V0.3 再做）
  → C 类视频通话，等状态机重做
```

---

## 11. 问题反馈模板

测试发现问题后，按此格式反馈给 Claude：

```
【测试项编号】#20 发圈带图
【操作】选图 → 发 → 刷新页面
【预期】图片仍在
【实际】刷新后图片变成裂图
【环境】Chrome 120 / Windows
【Network】（如有接口报错贴这里）
【Console】（如有 JS 报错贴这里）
```

---

## 附：文件索引

| 关注点 | 文件 |
|---|---|
| 后端入口（朋友圈 7 接口在这里） | `backend/api_server.py` |
| 朋友圈数据层 | `backend/moments.py` |
| 定时发圈线程 | `backend/moments_worker.py` |
| 速率限制 | `backend/rate_limit.py` |
| 错误码 + 统一信封 | `backend/errors.py` |
| LLM 调用 + emotion 解析 | `backend/core.py` |
| 朋友圈闭环测试脚本 | `backend/test_moments.py` |
| 前端接口封装 | `webview/src/api/backend.ts` |
| 朋友圈 store | `webview/src/stores/moments.ts` |
| 发圈页（图片 base64 修复） | `webview/src/views/ComposeMoment.vue` |
| V0.2 后端集成契约 | `BACKEND_INTEGRATION_V0.2.md` |
| V0.1 全量快照 | `PROJECT_SNAPSHOT.md` |

---

**一句话总结**：A 类后端测试全绿（61/61）；B 类前端测试需用户在浏览器走一遍，重点是发圈带图刷新验证（#20）和评论回复（#18）；C 类视频通话留 V0.3。Token 大头在图片对话（~8800/次）和文字对话（~8300/次），前端 UI 交互和语音 ASR 不消耗云 token。
