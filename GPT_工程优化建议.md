# Ling 项目工程优化建议

> **建议提出者：GPT（Codex）**  
> 生成日期：2026-07-13  
> 适用范围：`G:/personal project/ling/` 的 Vue WebView、FastAPI 后端、语音/视频通话链路，以及后续 Android APK 阶段。

## 一、结论

当前项目的 Web 前端可以构建，三个后端服务也能通过健康检查；但这只说明进程能够启动，不能证明聊天、语音和视频链路稳定。

**不建议现在进入 APK 安装阶段。** 应先完成以下 P0 问题的修复：

1. 明确 Android 客户端与 Python/模型后端的部署架构；
2. 修复消息“偶尔没有回复”的超时、重试、状态一致性问题；
3. 修复 WebSocket 通话无法可靠打断、TTS 音频重叠的问题；
4. 修复固定用户身份、开放 CORS 和局域网暴露导致的数据隔离与安全问题；
5. 增加自动化测试、日志和质量门禁。

## 二、已确认的现状

- Web 前端 `pnpm build` 已通过（Vue 类型检查与 Vite 构建通过）。
- 本地端口 `5173`、`8765`、`8766`、`8880` 均可连接；后端健康检查返回正常。
- 项目中没有 Android/Capacitor/Gradle/`AndroidManifest.xml` 等 APK 工程文件。
- 项目中没有可执行的前端单元测试、lint、格式化或 Python 依赖锁定配置。
- WebSocket 日志中已出现断连后继续接收消息，以及短音频导致 ASR 异常的记录。

## 三、为什么会“发消息不一定有回复”

这不是单一原因，更像是调用链缺少超时、重试、可观测性和状态收敛。

### 1. 外部 LLM 调用没有完整的故障治理

`/chat` 会同步等待 MiniMax 返回。网络抖动、模型限流、上游超时、返回格式异常，都可能使本次请求失败。前端目前只显示失败气泡，不能区分失败类型，也不能可靠重试。

### 2. 前端请求没有超时与取消机制

浏览器 `fetch` 默认可以长期等待。后端、模型或网络卡住时，用户会看到消息一直处于等待状态，主观感受就是“没有回复”。

### 3. 用户消息与助手回复不是一个可恢复的事务状态

用户消息会在调用 LLM 前写入历史。若 LLM 失败，用户消息会保留，但助手消息没有生成；刷新历史时就会看到“我发了消息，但她没回复”。

### 4. HTTP 与语音通话服务共享用户与数据

`api_server` 与 `ws_server` 分别创建后端实例，但使用固定用户身份并共享 SQLite/记忆数据。文字消息和通话并行时，存在历史顺序交叉、资源竞争和状态混乱风险。

### 5. 健康检查没有覆盖关键依赖

现有 `/health` 主要证明服务进程活着，不代表 MiniMax、TTS、数据库写入链路在当前时刻可用。因此 health 正常而聊天失败是可能的。

## 四、P0：先做诊断和可观测性

目标：把“偶尔没回复”变成可以定位、统计和复现的问题。

请按以下要求实现，不要先改业务人格或 UI：

1. 为每个 HTTP 请求生成 `request_id`。
2. 在 `/chat` 的入口、数据库写入前后、记忆检索前后、LLM 调用前后、LLM 解析后、响应返回前后记录结构化日志。
3. 每条日志至少包含：`request_id`、阶段名、耗时毫秒、异常类型、上游 HTTP 状态码（如有）。
4. 日志不得记录 API Key、完整聊天内容、完整 base64 图片/音频或其他隐私数据。
5. 编写独立测试用户的最小压测脚本：连续发送 20 条短消息，输出成功率、P50/P95/P99 耗时与失败分类。
6. 测试不得调用 `reset`，不得污染正式用户的聊天记录。
7. 先提交诊断结果与失败分类，再进行后续修复。

验收标准：每一次“无回复”都能用 `request_id` 找到原因，并能区分浏览器超时、后端异常、上游超时、上游限流、数据库异常和响应格式异常。

## 五、P0：定义稳定的聊天 API 契约

不要让前端依赖 `500 + 原始异常字符串`。建议统一为如下结构：

```ts
type ChatSuccess = {
  ok: true
  requestId: string
  reply: string
  emotion: Emotion
}

type ChatFailure = {
  ok: false
  requestId: string
  error: {
    code:
      | 'INVALID_INPUT'
      | 'UPSTREAM_TIMEOUT'
      | 'UPSTREAM_RATE_LIMITED'
      | 'UPSTREAM_UNAVAILABLE'
      | 'RESPONSE_INVALID'
      | 'INTERNAL_ERROR'
    message: string
    retryable: boolean
  }
}
```

实施要求：

1. 后端不再向客户端暴露原始异常、绝对路径、上游响应全文。
2. 输入文本设置上限，例如 2,000 字符；空白文本返回 `INVALID_INPUT`。
3. 为 LLM 设置明确连接超时和总超时。
4. 对连接失败、429、5xx、超时等可恢复错误，最多重试 2 次，采用指数退避和随机抖动。
5. 不重试用户输入错误等业务 4xx 错误。
6. LLM 返回空内容、非法 JSON 或空回复时，返回 `RESPONSE_INVALID`。
7. 前端按错误码显示用户可理解的中文提示；`retryable=true` 时提供重试入口。
8. 不使用 `any`、`@ts-ignore`、空 `catch` 或吞异常。

建议时间预算：前端总等待 35 秒，后端 LLM 总等待 25 秒，上游最多重试 2 次。

## 六、P0：修复消息一致性与重复发送

实施要求：

1. 每次发送生成 `client_message_id`（UUID）。
2. 后端接收 `client_message_id`，实现幂等：相同 `user_id + client_message_id` 的重试必须返回原结果，不能重复调用 LLM 或重复写库。
3. 消息增加明确状态：`pending`、`completed`、`failed`。
4. 用户消息与助手消息必须属于同一个可追踪的会话轮次。
5. LLM 失败时保留用户消息，但写入 `failed` 状态、错误码和是否可重试。
6. 成功后以明确事务/状态流程将本轮更新为 `completed`。
7. 刷新页面后不得把 `pending` 或 `failed` 伪装为正常完成消息。
8. 前端失败消息提供“重试”，不要求用户重新输入。
9. 为请求超时后重试、响应晚到、双击发送、刷新恢复状态增加测试。

## 七、P1：重构前端请求层

实施要求：

1. 统一封装请求函数：`AbortController`、35 秒超时、`request_id`、`client_message_id`、安全 JSON 解析、显式错误类型。
2. 路由切换、组件卸载、重复操作时取消不再需要的请求。
3. 不要只用一个全局 `sending` 控制全部状态；每条消息必须有独立发送状态。
4. 网络错误、超时、429、服务不可用必须显示不同提示。
5. 图片与 TTS 的 Object URL 在删除消息、清空会话和组件卸载时调用 `URL.revokeObjectURL`。
6. 增加 Vitest 测试：超时、取消、重试、重复点击、非法响应、刷新恢复 `pending/failed`。

## 八、P0：修复语音和视频通话状态机

当前 WebSocket 在收到 `stop` 后直接等待 ASR/LLM/TTS 处理完成，期间不能持续接收 `abort`，因此“打断”功能不可靠。多段 TTS 也缺少顺序播放队列，可能重叠播放。

实施要求：

1. WebSocket 的收包循环和单轮 ASR/LLM/TTS 处理拆为独立 `asyncio` task。
2. 每一轮通话带 `turn_id`；ASR、LLM chunk、TTS、done、aborted 均携带该 ID。
3. 收到 `abort` 后 500ms 内取消当前 LLM/TTS task、清空待播放音频，并发送 `aborted` 事件。
4. 前端仅处理当前 `turn_id` 的事件，忽略旧轮次延迟到达的数据。
5. 实现单一串行 TTS 播放队列，禁止多段音频同时播放。
6. 静音和扬声器按钮必须真正控制媒体轨/音频节点，而不是只修改 UI 状态。
7. 权限被拒绝时，不建立 WebSocket 通话；页面应回到可恢复状态并提示用户。
8. 空音频、短音频、断网、切换摄像头、挂断后都必须释放录音器、媒体轨、AudioContext、定时器和播放队列。

需要补充端到端测试：连续两轮说话、AI 说话中打断、断网重连、权限拒绝、挂断后无残留资源。

## 九、P0：身份隔离、安全和资源上限

当前固定 `DEVICE_ID = ling-web` 会让不同客户端共享会话；后端监听公网/局域网地址且 CORS 为 `*`，任何可访问该端口的设备都可能读取、写入或重置聊天记录。

实施要求：

1. 删除固定客户端身份；从受控身份体系取得 `user_id`。
2. 开发环境可显式模拟测试用户；生产环境必须鉴权。
3. CORS 改为环境白名单，不能使用 `*`。
4. 生产环境通过 HTTPS/WSS 网关或受控局域网访问，不能裸露 FastAPI 端口。
5. 限制文本长度、图片 MIME 类型和大小、音频时长、WebSocket 帧大小、并发连接数与每用户速率。
6. 对同一用户的写操作串行化，避免文字聊天和语音通话同时污染历史。
7. 为 LLM、VLM、TTS、ASR 设置并发上限；饱和时返回可重试错误，不可无限排队。

## 十、P1：部署与配置治理

实施要求：

1. 将 `Create-Ex`、ASR 模型、人格文件、TTS 地址等绝对路径全部改为环境配置。
2. 启动时校验模型目录、人格文件、API Key、数据库和 TTS 上游；任一关键依赖不可用时 fail fast。
3. 增加依赖锁定和可重复启动说明。
4. 区分 `development`、`LAN test`、`production` 三套配置。
5. 健康检查应区分 `ok`、`degraded`、`unavailable`，不能因为进程活着就一律返回 `ok`。
6. 加入 `pytest`、Vitest、ESLint、Prettier，以及 CI 质量门禁。

最低质量门禁：

```text
pnpm build
pnpm lint
pnpm test
pytest
```

## 十一、Android APK 的正确推进方式

当前项目无法直接生成可用 APK，因为 APK 内访问 `127.0.0.1:8765/8766` 指向的是手机自身，而 Python、FAISS、FunASR、GPT-SoVITS 也没有被打进 Android 工程。

必须先选择一种架构：

### 方案 A：APK 仅作为客户端（推荐）

- Vue WebView 使用 Capacitor 包装成 Android App；
- 后端、模型和数据库部署在服务器或可访问的局域网主机；
- APK 使用明确配置的 HTTPS API 地址与 WSS 地址；
- 配置 Android 的网络安全、麦克风、摄像头权限；
- 真机验证网络切换、后台恢复、权限拒绝、音频播放和摄像头。

### 方案 B：全部离线运行在手机端（当前不建议）

这不是简单打包 WebView，而是端侧模型、Python/原生推理运行时、模型压缩、设备性能、耗电、存储和 GPU/CPU 适配的重新设计。

## 十二、建议执行顺序

1. 先做“阶段四：诊断和可观测性”，找到无回复的实际失败分类；
2. 再做聊天 API 契约、超时、重试、幂等和消息状态；
3. 修复 WebSocket 打断与 TTS 队列；
4. 补测试、lint、格式化和部署配置；
5. 完成安全与身份隔离；
6. 选择 APK 架构；
7. 最后才创建 Capacitor Android 工程和真机测试。

## 十三、APK 前验收标准

满足以下条件后才进入 APK：

1. 连续发送 20 次文本消息成功率达到 100%，并能给出 P95 响应时间；
2. 模拟模型超时、429、断网时，用户始终获得明确状态和重试入口；
3. 双击发送、页面刷新、网络重连不会重复写入或混乱历史；
4. 语音通话打断生效，TTS 不重叠，挂断后无残留录音、媒体轨、定时器或音频；
5. 不同用户的聊天历史严格隔离；
6. `pnpm build`、`pnpm lint`、`pnpm test`、`pytest` 全部通过；
7. 后端 API 地址和 WebSocket 地址不再硬编码为手机不可用的 `127.0.0.1`。
