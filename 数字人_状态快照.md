# 数字人视频合成 - 状态快照

> 本文档供"数字人视频合成"专题对话使用。ling 项目主对话在专心做后端升级，数字人方案由独立对话推进。

---

## 一、项目背景

**ling** 是一个 AI 陪伴应用（人格"刘嘉玲"），Vue 3 + FastAPI 双栈。

当前已有**语音/视频通话**功能（V0.3 已完成全双工改造），但"视频通话"里对方只有静态头像 + emotion 切换，没有真正的动态视频形象。本次目标是做**数字人**：让对方（刘嘉玲）在视频通话里有动态表情和口型。

---

## 二、当前视频通话实现（数字人需在此基础上改）

### 关键文件
- `webview/src/views/VideoCall.vue` — 通话主界面（3 阶段：choose/calling/active）
- `webview/src/components/EmotionAvatar.vue` — 情绪头像组件（当前是静态图 + emotion 切换）
- `webview/src/api/backend.ts` — VoiceCallClient（WS 协议）
- `webview/src/utils/ttsQueue.ts` — TTS Opus 包解码播放队列

### 当前"视频通话"视觉呈现
```
┌─────────────────────────┐
│      [静态头像]          │  ← EmotionAvatar，靠 emotion 信号切换图
│      刘嘉玲              │
│      00:42               │
│      正在说话…            │  ← isSpeaking 时显示
│                         │
│   "她: 宝贝想你了"        │  ← 字幕 subtitle
│                         │
│                  [小窗]  │  ← 自己的摄像头 localVideo
│  [静音][摄像][翻转][扬声器][挂断] │
└─────────────────────────┘
```
**问题**：视频通话和语音通话视觉上没区别，对方不是"视频"而是"带情绪切换的头像"。

### Emotion 驱动信号（已有，数字人可直接复用）
后端 WS 下行协议已支持 emotion 信号：
```json
{"type":"emotion","emotion":"日常|调情|撒娇|焦急|冷淡","sentence_id":"..."}
```
- **初始 emotion**：每轮开始前发 `"日常"`（`core.py:224 yield ("", "日常")`）
- **最终 emotion**：LLM 回复完后用关键词规则猜（`core.py:293 _guess_emotion`）
- 5 种情绪：日常 / 调情 / 撒娇 / 焦急 / 冷淡

### TTS 播放时机（口型同步要用）
- `onTtsStart` — 一句 TTS 开始（口型该动了）
- `onTtsAudio(opusBytes)` — 逐个 Opus 包入队（可据此做口型开合节奏）
- `onTtsStop(reason)` — 一句结束（"aborted"=被打断，否则=正常句末）
- `isSpeaking` ref — TTS 播放中为 true（口型动 / 不动的总开关）

### 前端 VAD（已有）
`VideoCall.vue:205 startFrontendVad()` — 用 AnalyserNode 检测用户音量，超阈值且 isSpeaking 时触发打断。数字人可复用这套音量分析做"用户说话时她安静听"的状态。

---

## 三、数字人需求（用户原话）

> 我提供多张姿势不同情绪表情的照片，然后画面通过情绪驱动去产生表情，**重点是照片过渡上要平滑**。

### 核心诉求
1. **输入**：用户提供的多张照片（不同姿势/情绪/表情）
2. **驱动**：emotion 信号（已有 5 种）+ TTS 播放状态（口型）
3. **关键**：照片间过渡平滑（不是生硬切换）

---

## 四、方案建议（按实现难度排序）

### 方案 A：情绪分层 + Crossfade（推荐先做，1-2 天）

**思路**：每种情绪一张基准照片，情绪变化时 opacity 交叉淡入淡出；说话时叠口型动效层。

**实现**：
- 5 种情绪各一张基准照片（日常/调情/撒娇/焦急/冷淡）
- 两层 `<img>` 叠放，emotion 变化时新层 opacity 0→1，旧层 1→0（CSS transition 300-500ms）
- 说话时在嘴部区域叠一个"口型开合"层：
  - 简单版：CSS clip-path 或 transform scaleY 做嘴部开合，按 TTS Opus 包到达节奏动
  - 进阶版：canvas 在嘴部画一个椭圆，大小随音频 RMS 变化（复用前端 VAD 的 AnalyserNode）
- `isSpeaking` 控制口型动/不动

**优点**：实现简单，过渡平滑，不依赖额外库
**缺点**：照片是切换不是真"动"，口型是叠加层不是照片本身变形
**适用**：快速出效果，验证体验

### 方案 B：Live2D Cubism 式分层（效果好，3-5 天）

**思路**：把照片在 PS 里分层（脸/眼/嘴/眉毛/头发），导入 Live2D Cubism，用参数驱动各层做表情。

**实现**：
- 照片分层（手动，每种情绪一套或共用一套+参数）
- Live2D Cubism Editor 绑定网格 + 参数（嘴开合、眉毛、眼眨）
- 前端用 `pixi-live2d-display` 渲染
- emotion 信号 → Live2D 参数组合
- TTS 节奏 → 嘴开合参数

**优点**：真动，表情细腻，口型同步自然
**缺点**：分层工作量大，需学 Live2D，照片分层需美术基础
**适用**：追求真实感

### 方案 C：关键点形变（canvas + face landmarks，2-3 天）

**思路**：用 face-api.js 或 MediaPipe Face Mesh 检测照片人脸关键点，情绪驱动关键点位移，canvas 重绘变形。

**实现**：
- `@tensorflow-models/face-landmarks-detection` 或 `@mediapipe/face_mesh` 检测 468 个关键点
- 每种情绪定义一组关键点偏移量（嘴角上扬=笑，眉毛下压=急）
- canvas 用 mesh warp 按关键点变形照片
- 过渡：关键点偏移量插值（300ms）

**优点**：不用手动分层，一张照片可出多种表情
**缺点**：变形可能失真，大角度侧脸效果差，性能开销中等
**适用**：照片少、想用算法生成表情

### 方案 D：AI 生成过渡帧（质量最高，最慢）

**思路**：用 Stable Diffusion img2img / AnimateDiff 在两张照片间生成中间帧。

**优点**：最自然
**缺点**：实时性差（每帧 1-2s），需 GPU，不适合实时通话
**适用**：离线生成表情库，通话时播放

---

## 五、推荐路径

**先做方案 A**（情绪 crossfade + 口型叠加层），1-2 天出效果验证：
- 照片过渡平滑问题用 CSS opacity transition 解决（300-500ms 交叉淡入）
- 口型用 AnalyserNode 音量驱动一个叠加层（复用前端 VAD 已有的音量分析）
- emotion 驱动复用现有 WS 信号

**体验达标后**，若要更真实再升级方案 B（Live2D 分层）。

方案 C/D 不推荐起步——C 的变形失真风险高，D 实时性不够。

---

## 六、数字人需对接的现有接口

### 输入信号（数字人消费）
| 信号 | 来源 | 用途 |
|---|---|---|
| `emotion` | WS `{"type":"emotion","emotion":"撒娇"}` | 切换基准照片/表情 |
| `isSpeaking` | `VideoCall.vue` ref | 口型动/不动总开关 |
| TTS 音频包 | `onTtsAudio(opusBytes)` → `ttsQueue` | 口型开合节奏（包到达频率） |
| 用户音量 | 前端 VAD AnalyserNode（已有） | 她安静听的状态 |

### 待替换组件
- `EmotionAvatar.vue` → 升级为 `DigitalHuman.vue`（或保留 EmotionAvatar 做降级）
- `VideoCall.vue` 的 `.main-view` 区域（当前放头像）→ 放数字人

### 照片资源
- 放 `webview/public/digital-human/` 或 `webview/src/assets/digital-human/`
- 命名建议：`daily.png` / `flirt.png` / `coquettish.png` / `anxious.png` / `cold.png`
- 尺寸建议：统一比例（如 1:1 或 3:4），分辨率 ≥ 512px

---

## 七、口型同步细节（方案 A 进阶）

TTS 是 Opus 60ms 包流式播放，口型可这样做：

```ts
// 复用 ttsQueue 的播放状态 + AnalyserNode 分析播放音量
// 当 isSpeaking && 播放音量 > 阈值 → 嘴张开
// 嘴开合幅度 = 音量 RMS 映射

// 简化版：用 TTS 包到达频率做开合（包密=说快=嘴动快）
let mouthOpen = 0
onTtsAudio(() => {
  mouthOpen = Math.min(mouthOpen + 0.3, 1)  // 收到包=张嘴
})
// 每 60ms 衰减
setInterval(() => { mouthOpen *= 0.7 }, 60)
// canvas 画嘴：椭圆高度 = mouthOpen * maxHeight
```

或更准：用 `AudioContext.createAnalyser()` 接 TTS 播放的 `AudioBufferSourceNode`，实时拿 RMS 驱动嘴型（但需要改 ttsQueue 暴露音频节点）。

---

## 八、ling 项目约束（数字人实现需遵守）

- 包管理器固定 **pnpm**（不用 npm/yarn）
- 无 Prettier，格式以 eslint + 2 空格/行宽 120/单引号/末尾不加分号为准
- 组件用 `<script setup lang="ts">` + Composition API
- 不用 `any`（原型 throwaway 可豁免）、不用 `@ts-ignore`
- 新增依赖前确认（Live2D/face-api 等需先和用户确认）
- 中文注释

---

## 九、主对话当前进度（避免冲突）

主对话正在做后端升级（VLM 串行优化、视频帧处理等），**不碰前端 VideoCall.vue 的视觉层**。数字人对话可放心改 `VideoCall.vue` 的 `.main-view` 区域和 `EmotionAvatar.vue`。

若数字人需要后端新增信号（如口型节奏数据、更细的表情参数），主对话可配合在 WS 协议加字段。
