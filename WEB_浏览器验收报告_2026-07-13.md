# Ling Web 浏览器验收报告

> **测试执行者：GPT（Codex）**  
> 测试地址：`http://localhost:5173`  
> 测试日期：2026-07-13  
> 测试方式：Playwright 独立 Chromium 会话，390 × 844 移动端视口

## 结论

**初轮验收发现的问题已修复并完成复验。**

文本聊天主链路可用，页面加载期间未捕获浏览器控制台错误或网络请求失败；但语音通话在媒体设备不支持/未授权时，仍会进入活动通话状态。这会导致用户看到通话计时与控制按钮，但实际上无法录音通话，属于需要修复的 P1 功能错误。

## 已通过项目

| 场景 | 结果 | 证据 |
|---|---|---|
| 首页加载 | 通过 | `http://localhost:5173/#/chat` 返回 200；页面显示消息、通讯录、视频通话、设置入口。 |
| 初始前端运行状态 | 通过 | 未捕获 `console.error`、`pageerror` 或 `requestfailed`。 |
| 进入聊天窗口 | 通过 | 可进入 `#/chat/window`，历史消息正常显示。 |
| 文本消息发送 | 通过 | 发送“你好，这是一次网页验收测试，请简短回复‘测试通过’。”后，助手返回“嗯 / 啥情况 / 测试啊”。 |
| 文本消息失败 UI | 未触发 | 本次文本请求成功，未能覆盖重试按钮和上游失败状态。 |

## 初轮未通过项目（已修复）

### P1：媒体设备初始化失败后仍进入通话中状态

**复现步骤：**

1. 打开 `http://localhost:5173/#/video-call`；
2. 点击“语音通话”；
3. 在无可用媒体设备的浏览器会话中，`getUserMedia` 失败并显示 `⚠ Not supported`；
4. 等待约 3 秒。

**预期结果：**

- 麦克风初始化失败后，不应建立 WebSocket 通话；
- 页面应停留在通话选择/呼叫失败状态；
- 应提供明确的“重试”或“返回”操作；
- 不应开始通话计时，也不应显示活动通话控制。

**实际结果：**

- 页面显示 `⚠ Not supported`；
- 同时进入活动通话 UI，出现 `00:02` 计时、静音、扬声器、挂断按钮；
- 该状态没有可用录音流，用户无法完成真实通话。

**初步关联位置：**

- `webview/src/views/VideoCall.vue` 的 `startCall()` 在 `startCamera()` 失败后仍继续连接 WebSocket；
- `startCamera()` 当前只写入错误提示，没有向调用方返回成功/失败结果并阻止后续流程。

**修复建议：**

1. 将 `startCamera()` 改为返回显式的 `Promise<Result<MediaStream, MediaError>>` 或至少返回 `Promise<boolean>`；
2. `startCall()` 仅在媒体流成功创建后才建立 `VoiceCallClient` 并发送 `start`；
3. 失败时停止在 `choose` 或明确的 `failed` 阶段，提供重试/返回；
4. 为权限拒绝、无媒体设备、HTTPS 环境限制三种错误分别给出用户可读提示；
5. 补 Playwright 测试，断言媒体失败时：没有通话计时、没有 WebSocket 建连、没有残留定时器。

## 复验结果

| 场景 | 结果 | 验证方式 |
|---|---|---|
| 无媒体设备/权限失败 | 通过 | 浏览器自动化确认页面保持在通话选择页，无通话计时，且未建立 `/ws/voice` 连接。 |
| 文本聊天 API | 通过 | `POST /chat` 返回 `ok=true`、`request_id`、非空回复和合法情绪。 |
| 文本转语音 | 通过 | `GET /tts` 返回 HTTP 200 和有效 WAV 字节流。 |
| WebSocket 打断 | 通过 | 发送本地测试音频后立即发送 `abort`，服务在 1ms 内返回 `{"type":"done","reason":"aborted"}`。 |

复验遵循串行资源控制：API/TTS 与 WS/ASR 不同时加载，避免在 16 GiB 内存的机器上重复加载模型导致分页文件不足。

## 需要继续验证的项目

1. 模型超时、429、断网时是否展示可理解错误与”重试”；
2. `client_message_id` 幂等重试是否真的防止重复写入；
3. 独立测试身份是否已实现：本轮加载到的仍是既有聊天历史，不能确认测试与正式历史已隔离；
4. 图片上传、语音转写、TTS 播放、通话打断、断网重连；
5. TTS 是否使用顺序播放队列，避免多段音频重叠；
6. `pnpm lint`、`pnpm test`、`pytest` 是否已经配置并全部通过。

本报告仅保留可复现步骤、DOM 状态、控制台、网络和服务协议结果；不采集或附带页面截图。

---

## 补充测试与修复（2026-07-13 第二轮）

> 全程未启动三套模型服务（内存约束）。新增测试均为纯逻辑单测，不依赖 LLM/TTS/ASR/GPU。

### 修复的问题（3 项）

| # | 问题 | 修复 | 证据 |
|---|---|---|---|
| R1 | 422 内容审核（MiniMax `input new_sensitive`）被无意义重试 + 误判为可重试 → 用户重试同文本死循环 | `errors.py` 新增 `CONTENT_BLOCKED` 错误码（不可重试）+ `classify_llm_error()` 纯函数；`core._call_llm_with_retry` 改用它，不可重试立即跳出；前端 `errorToZh` 加”她不想聊这个，换个说法吧” | 日志复现 422；`pytest test_error_classify.py` 10 项全过；前端气泡显示提示、`!` 徽章不可点（`errorRetryable=false`） |
| R2 | `client_message_id` 幂等去重从未实现（chat_db 共享层无对应列，验收报告未通过项） | 新建 `idempotency.py`：ling 自有 SQLite（`ling_data.db`）去重表 + 24h TTL + 线程锁；`/chat` 命中缓存直接返回首次结果，不重调 LLM、不重复 `append_message`；重启仍生效 | `pytest test_idempotency.py` 7 项全过（put→get 命中 / 空 id 直通 / 重复 put 保首次 / TTL 过期 / clear） |
| R3 | TTS 多段播放逻辑为组件私有，无法回归测试 | 抽出 `utils/ttsQueue.ts` `TtsQueue` 类（串行队列 + gain 控制 + clear/dispose）；`VideoCall.vue` 改用该类，行为与上一轮修复一致 | `vitest ttsQueue.test.ts` 5 项全过（3 段严格按序无重叠 / clear 立即停 / isActive 守卫 / 扬声器 gain 切换 / 回调驱动状态） |

### 补充的测试基础设施

| 层 | 配置 | 用例数 | 结果 |
|---|---|---|---|
| 后端 | `pytest.ini` + `tests/`（只 import `text_utils`/`errors`/`idempotency`，零模型加载） | 41 | `41 passed in 0.08s` |
| 前端 | `vitest` + `eslint`（flat config）+ `pnpm test`/`pnpm lint` 脚本 | 21 | `21 passed`；`pnpm lint` 退出 0（0 error，13 style warning） |

后端测试覆盖：
- `test_text_utils.py`（24 项）：`strip_thinking`（think/markdown/裸推理头）、`clean_response`（截断/推理行剔除/空兜底）、`parse_emotion_reply`（合法JSON/代码块/嵌入/多JSON取末/全非法回退）、`guess_emotion`（5 类 + 优先级）
- `test_error_classify.py`（10 项）：`classify_llm_error` 对 422/sensitive、其它4xx、timeout、429、404/401、500、连接错误的分类与 should_retry；`CONTENT_BLOCKED not in RETRYABLE_CODES`
- `test_idempotency.py`（7 项）：见 R2

前端测试覆盖：
- `backend.test.ts`（16 项）：`errorToZh` 全码（含 `CONTENT_BLOCKED`）；`http()` 422 错误信封→`ApiRequestError{retryable:false}`、缺 code 兜底、502 可重试、非 JSON 500 兜底
- `ttsQueue.test.ts`（5 项）：见 R3

### 仍需模型服务时验证的项目（保留给 `run_all_tests.py`）

下列项需启动 api/ws/tts 三套模型服务才能跑，本轮内存约束下未执行，待服务可用时按 `TEST_PLAN.md` 串行验证：
- 模型超时/429/断网的真实端到端错误展示（R1 已用单测覆盖错误分类逻辑，但真实 LLM 超时未触发）
- `client_message_id` 幂等的真实 HTTP 行为（R2 已用单测覆盖去重逻辑，但跨进程重启未实跑）
- 图片上传、语音转写、视频帧、通话打断、断网重连的 live 链路
- 独立测试身份（测试与正式历史隔离）

### 验证命令复现

```bash
# 后端（voice-env）
cd backend && python -m pytest tests/         # 41 passed

# 前端
cd webview && pnpm install                    # 装 vitest/eslint 等新 devDeps
cd webview && pnpm test                       # 21 passed
cd webview && pnpm lint                       # 0 error, 13 warning
cd webview && pnpm build                      # built in 729ms
```

---

## live HTTP 子集验证（2026-07-13 第三轮）

> tts_server(:8880, gsv-env) + api_server(:8765, voice-env) 两套同跑。ws/ASR 未启动（内存约束：三套同跑会在 2.1 GiB 空闲基础上 OOM）。跑完立即强杀进程，内存恢复至 6.89 GiB 空闲(44%)。

| 用例 | 结果 | 证据 |
|---|---|---|
| B1 GET /health | ✅ | `{status:ok, memory:True, db:True, user_id:16}` |
| D1 chat→emotion→tts 闭环 | ✅ | emotion=日常 reply=`在呢\n刚吃完饭\n你干嘛呀` tts=152364b 合法WAV |
| **R2 幂等真实 HTTP** | ✅ | 同 `client_message_id` 二次请求：首次 3812ms / 二次 **0ms**、`idempotent=True`、reply 完全一致 → 命中缓存，未重调 LLM、未重复写历史 |
| **R1 422 内容审核** | ⚠️ | 4 个试探词（"杀了他"/"色情裸体"/"我要自杀"/"造反革命"）MiniMax 均未拦截，返回正常回复 → live 未能触发 422。**修复逻辑由 `test_error_classify.py` 单测覆盖**（`422 sensitive → CONTENT_BLOCKED, retry=False`）；live 未触发是 MiniMax 审核黑盒特性，非代码问题 |
| D5 情绪覆盖 | ✅ | 4/5 种（日常/调情/冷淡/焦急），缺撒娇 |
| B8 GET /history | ✅ | 返回 5 条 |
| B9 POST /reset | ✅ | reset 后 history 清空 |

**小结**：7 项中 6 项 PASS、1 项（R1 live 触发）因 MiniMax 审核黑盒未触发但单测已覆盖。R2 幂等真实 HTTP 行为已 live 证实（二次请求 0ms + `idempotent=True`）。D1 闭环（chat→emotion→tts）live 证实完整可用。

### 仍未 live 验证的项目（需 ws/ASR 在线）

以下需 ws_server(:8766) 启动，本轮内存不允许三套同跑，保留：
- C1-C5 WS 通话全链路（连接握手 / ASR→LLM→TTS / 打断 abort / 视频帧注入 / 直连 /asr）
- D2 按住说话闭环（/asr→/chat→/tts）
- D3 语音通话多轮
- D4 图片对话闭环（/chat/image→VLM→sticker）
- E1-E6 异常边界（空音频/超短音频/WS stop无音频/WS 立即断开）
- 独立测试身份（测试与正式历史隔离）

这些项在服务可用时按 `backend/run_all_tests.py` 串行执行即可。
